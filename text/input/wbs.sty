% ============================================================================
%
%   packages 2.sty, Fabian Gunzinger
%
%   This file loads all basic packages required to compile latex.
%
% ============================================================================


% Basic packages
\usepackage{
    % amssymb,
    % amsfonts,
    % amsmath,
    % bbm,
    % calc,
    % lscape,
    % epstopdf,
    % enumerate,
    % enumitem,
    % endnotes,
    % graphicx,
    % latexsym,
    % morefloats,
    setspace,
    % fancyhdr,
    % url,
    % float,
    % caption,
    % subcaption,
    % tabularx,
    % threeparttable,
    % dcolumn,
    % booktabs,
    % tikz,
    % xfrac,
    % xcolor
}

\usepackage[utf8, ansinew]{inputenc}    % required for umlaute
\usepackage{csquotes}
\usepackage[ngerman, english]{babel}    % required for umlaute
\usepackage[T1]{fontenc}                % required for umlaute
% \usepackage[section]{placeins}
% \usepackage{placeins}
% \usepackage{adjustbox}

% Hyper refs
\usepackage[
    colorlinks,
    breaklinks,
    bookmarks=false,
    pdfstartview=Fit,  % for fitting entire page; FitW just fits width
    pdfview=Fit,       % after traversing a hyperlink
    linkcolor=black,
    urlcolor=blue,
    citecolor=black,
    hyperfootnotes=false
]{hyperref}
% \usepackage[figure,table]{hypcap}

% Bibliography style settings.
\usepackage[
	backend=biber,
    bibencoding=utf8,
	style=authoryear,
	maxcitenames=2,
    maxbibnames=30,
	isbn=false,
	eprint=false,
	url=true
]{biblatex}
\renewcommand*{\multicitedelim}{,\space}
\DeclareFieldFormat[techreport,report,unpublished]{title}{\mkbibquote{#1}}
\AtEveryBibitem{% Clean up the bibtex rather than editing it
 \clearfield{date}
 \clearfield{eprint}
 \clearfield{isbn}
 \clearfield{issn}
 \clearfield{month}
 \clearfield{note}
 \clearfield{language}
 \ifentrytype{article}{
  \clearfield{url}
  \clearfield{doi}
 }
}

% Block quotes
% \usepackage{etoolbox}
% \AtBeginEnvironment{quote}{\singlespacing\small}

% Command shortcuts
\newcommand{\citet}[1]{\textcite{#1}}
\newcommand{\citep}[1]{\parencite{#1}}
\newcommand{\citef}[1]{\footcite{#1}}
\newcommand{\edit}[1]{{\noindent\color{red}{#1}}}
\newcommand{\gap}{{\color{red}...\#...}}
% \newcommand{\note}[1]{\center\scriptsize Note: #1}
\newcommand{\source}[1]{\center\scriptsize Source: #1}
\newcommand{\what}[1]{{\noindent\textit{\color{blue}{#1}}}}

% Title case format
% \captionsetup{labelfont=bf, font=bf}
